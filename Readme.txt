RFD节点通电后检查是否在网络中
	在网络中,则:
		等待RF消息
			根据收到的RF消息中的地址判断是否要接收此封包
		每隔固定时间就广播一个包含自身地址的心跳包,以证明存活
		
	不在网络中,则:
		每隔固定时间,广播一个注册消息,请求加入网络,等待返回消息
			若太长时间未收到回复,则返回上一级
			若收到注册成功消息则标记自身的地址

		
FFD节点通电后不断监听RF消息和PC端消息
	若是RF注册请求消息,则:
		分配地址,发送一个注册成功消息